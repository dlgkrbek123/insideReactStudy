------------------------------------------------------

useEffect로 부수효과를 처리할 수 있긴함
- 근데 빈화면 보이다가 바로 다시 그려지는 깜빡임이 아쉬움
- 커밋 단계 이후 비동기 실행

useLayoutEffect
- 화면에 그려지기 직전에 동기적으로 실행
- paint이전에 동기적으로 실행
- 페인트를 지연시킬수 있으므로 무분별한 사용은 조심

------------------------------------------------------